export interface RenewableData {
    entity: string; // Nombre de la entidad o región
    code: string | null; // Código opcional de la región
    year: number; // Año del registro
    renewables: number; // Porcentaje de energía renovable
  }
  